name: CI
 
on:
  push:
    branches:
      - main
 
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
 
    - name: Build Java application
      run: javac Main.java

    - name: Build Java application
      run: javac Main.java
 
    - name: Build Docker image
      run: docker build -t keerthikolati/my-java-app .
 
    - name: Push Docker image
      run: |
        docker login -u keerthikolati -p dckr_pat_WNAC2pxJKswhxkJ-Pm1sDgPH4U0
        docker push keerthikolati/my-java-app
