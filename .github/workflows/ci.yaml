name: Java CI
 
on:
  push:
    branches:
      - main
 
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
 
    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'

    - name: Navigate to Dockerfile directory
      run: cd javaapp/docker-file
      #working-directory: ${{ github.workspace }}  # Specify the workspace directory
 
    - name: Build Docker image
      run: docker build -t keerthikolati/my-java-app:v1.0 .
 
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: keerthikolati
        password: dckr_pat_WNAC2pxJKswhxkJ-Pm1sDgPH4U0
 
    - name: Push Docker image
      run: |
        docker push keerthikolati/my-java-app:v1.0
